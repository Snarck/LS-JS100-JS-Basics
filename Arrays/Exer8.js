/*


Type

How can you check whether a variable holds a value that is an array? For example
imagine you start writing a function and want to check whether its argument is an array:6

function filter(input) {
  // Is input an array?
}

*/

let arr = [1, 2, 3];
console.log(Array.isArray(arr));

//I was right.